// extenders

"use strict"

// Imports.
const Stream = require("./Stream")

// createObjects :: [String] -> Stream [String] -> Stream (Object String a)
const createObjects = x => x

// Exports.
module.exports = {
  createObjects: createObjects
}
